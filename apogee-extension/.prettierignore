dist/
node_modules/
content/Readability.js
*.min.js
# Extractor fixtures stand in for real captured pages, and whitespace in them is
# test data: extractors read innerText, so reflowing the markup can change what
# a test asserts. Keep them byte-for-byte as saved.
tests/extractors/fixtures/
